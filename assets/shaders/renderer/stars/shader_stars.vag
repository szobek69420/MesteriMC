#version 330 core
layout(location=0) in vec3 aPos;
layout(location=1) in mat4 instanceModelMatrix;

uniform vec3 playerPos;
uniform mat4 pv;

void main()
{
	gl_Position=pv*(instanceModelMatrix*vec4(aPos,1.0)+vec4(playerPos,1.0));
	gl_Position.z=gl_Position.w;
}