#version 330 core

in VS_OUT{
    vec2 TexCoords;
    mat3 tbn;
}fs_in;

out vec4 fragColour;

uniform sampler2D texture_albedo;
uniform sampler2D texture_normal;

void main()
{
    fragColour=vec4(texture(texture_albedo, fs_in.TexCoords).rgb, 1.0);
}