#version 330 core
layout (location = 0) in float vertex; // ingore it
out vec2 TexCoords;

uniform mat4 vertexData;
uniform mat4 projection;

void main()
{
    vec4 vec=vertexData[gl_VertexID].xyzw;
    gl_Position = projection * vec4(vec.xy, 0.0, 1.0);
    TexCoords = vec.zw;
}  